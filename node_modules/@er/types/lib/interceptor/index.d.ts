import { HttpRequest } from '@angular/common/http';
export interface HttpInterceptorConfig {
    urlPatterns: string | RegExp | string[] | RegExp[];
    request?: (req: HttpRequest<any>) => HttpRequest<any>;
    response?: (response: any) => any;
}
