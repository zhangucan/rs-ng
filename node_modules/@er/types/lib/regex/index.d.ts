export declare const RegPatterns: {
    rule: RegExp;
    digit: RegExp;
    letter: RegExp;
    character: RegExp;
    chinese: RegExp;
    integer: RegExp;
    pInteger: RegExp;
    numeric: RegExp;
    pNumeric: RegExp;
    /**
     * @descrition:邮箱规则
     * 1.邮箱以a-z、A-Z、0-9开头，最小长度为1.
     * 2.如果左侧部分包含-、_、.则这些特殊符号的前面必须包一位数字或字母。
     * 3.@符号是必填项
     * 4.右则部分可分为两部分，第一部分为邮件提供商域名地址，第二部分为域名后缀，现已知的最短为2位。最长的为6为。
     * 5.邮件提供商域可以包含特殊字符-、_、.
     */
    email: RegExp;
    /**
     * [ip ipv4、ipv6]
     * "192.168.0.0"
     * "192.168.2.3.1.1"
     * "235.168.2.1"
     * "192.168.254.10"
     * "192.168.254.10.1.1"
     */
    ip: RegExp;
    /**
     * @descrition:判断输入的参数是否是个合格的固定电话号码。
     * 待验证的固定电话号码。
     * 国家代码(2到3位)-区号(2到3位)-电话号码(7到8位)-分机号(3位)
     **/
    fixPhone: RegExp;
    /**
     *@descrition:手机号码段规则
     * 13段：130、131、132、133、134、135、136、137、138、139
     * 14段：145、147
     * 15段：150、151、152、153、155、156、157、158、159
     * 17段：170、176、177、178
     * 18段：180、181、182、183、184、185、186、187、188、189
     * 国际码 如：中国(+86)
     */
    phone: RegExp;
    /**
     * @descrition 匹配 URL
     */
    url: RegExp;
    /**
     * 金额
     */
    money: RegExp;
    ascii: RegExp;
    no_space: RegExp;
    /**
     * 全角
     */
    fullWidth: RegExp;
    /**
     * 半角
     */
    halfWidth: RegExp;
    percent: RegExp;
    date_yyyy_mm_dd: RegExp;
    date_yyyymmdd: RegExp;
    role: RegExp;
    permission: RegExp;
};
