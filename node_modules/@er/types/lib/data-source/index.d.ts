import { Query } from 'elastic-builder';
import { AggInterval } from '../aggs';
import { ApiDataProps } from '../api-data';
export interface DataSource {
    $id?: any;
    apiDataProps: ApiDataProps | ApiDataProps[];
    query?: Query | Query[];
    queryValue?: string;
    interval?: AggInterval | string;
    format?: string;
}
