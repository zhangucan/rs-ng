import { ErFormlyFieldConfig } from '@er/formly';
import { FormProps } from '@er/types';
import { FormlyFormOptions } from '@ngx-formly/core';
import { MenuItem } from 'primeng/api';
export interface StepForm extends MenuItem {
    fields: ErFormlyFieldConfig[];
    options?: FormlyFormOptions;
}
export interface WizardFormProps extends FormProps {
    activeIndex: number;
    stepStyleClass: string;
    stepStyle: string;
    stepChange: Function;
    steps: StepForm[];
}
