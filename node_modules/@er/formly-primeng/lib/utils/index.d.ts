import { ErFormlyFieldConfig } from '@er/formly';
import { PngAutoCompleteProps } from '@er/primeng';
import { FormProps } from '@er/types';
export declare class PngFormlyUtils {
    static resolveField(formProps: FormProps, field: ErFormlyFieldConfig, formlyForm?: any): void;
    static distinctSuggestProps(apiEntry: any, fieldName: any, query?: any, selectable?: boolean): {
        type: string;
        props: PngAutoCompleteProps;
        templateOptions: {
            required: boolean;
            description: string;
        };
    };
    private static resolveValidators;
    private static resolveBasic;
    private static resolveProps;
    private static resolveAttrs;
    private static resolveTemplateOptions;
    private static resolveValidations;
    private static resolveParsers;
    private static resolveDataField;
}
