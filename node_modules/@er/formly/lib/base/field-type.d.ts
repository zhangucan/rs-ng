import { AfterViewInit, OnInit, QueryList, ViewContainerRef } from '@angular/core';
import { AbstractControl } from '@angular/forms';
import { ValidationMessageDirective } from '@er/validate';
import { FieldType } from '@ngx-formly/core';
import { ErFormlyFieldConfig } from './field-config';
export declare abstract class ErFormlyFieldType extends FieldType<ErFormlyFieldConfig> implements OnInit, AfterViewInit {
    defaultProps: {};
    widgets: QueryList<ViewContainerRef>;
    validateMessage: ValidationMessageDirective;
    readonly attrs: {};
    readonly props: {};
    readonly context: any;
    value: any;
    component(key?: any): any;
    data$(key?: any): any;
    control(key?: string | string[]): AbstractControl;
    fieldProps(key: any): any;
    ngAfterViewInit(): void;
    ngOnInit(): void;
}
