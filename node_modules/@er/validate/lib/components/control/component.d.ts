import { ChangeDetectorRef, OnDestroy, OnInit, TemplateRef } from '@angular/core';
import { ControlContainer, FormControl } from '@angular/forms';
import { Subscription } from 'rxjs';
import { BehaviorSubject } from 'rxjs/internal/BehaviorSubject';
export declare class ValidateMessagesComponent implements OnInit, OnDestroy {
    private controlContainer;
    private cd;
    control: FormControl;
    controlName: string;
    formlyField: any;
    force: boolean;
    styleClass: string;
    showAllErrors: boolean;
    messages: {
        [key: string]: string;
    };
    errorMsgTpl: TemplateRef<any>;
    errors$: BehaviorSubject<string[]>;
    subscription: Subscription;
    constructor(controlContainer: ControlContainer, cd: ChangeDetectorRef);
    getErrorMessages(): void;
    getFirstError(errorMessages: any): any;
    getErrors(errorMessages: any): any[];
    ngOnInit(): void;
    ngOnDestroy(): void;
}
