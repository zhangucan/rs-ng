import { OnDestroy, OnInit, TemplateRef } from '@angular/core';
import { ControlContainer, FormGroup } from '@angular/forms';
import { Subject, Subscription } from 'rxjs';
export declare class FormValidateMessagesComponent implements OnInit, OnDestroy {
    private controlContainer;
    form: FormGroup;
    messages: {
        [key: string]: string;
    };
    fields: {};
    styleClass: string;
    errorMsgTpl: TemplateRef<any>;
    errors$: Subject<{}>;
    subscription: Subscription;
    constructor(controlContainer: ControlContainer);
    checkFormValidate(): void;
    ngOnInit(): void;
    errorKeys(errors: {}): string[];
    errorMsg(error: any): any;
    ngOnDestroy(): void;
}
