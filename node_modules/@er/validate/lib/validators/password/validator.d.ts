import { ValidatorFn } from '@angular/forms';
export interface PasswordValidatorOptions {
    minLength?: number;
    maxLength?: number;
    requireLetters?: boolean;
    requireLowerCaseLetters?: boolean;
    requireUpperCaseLetters?: boolean;
    requireNumbers?: boolean;
    requireSpecialCharacters?: boolean;
}
export declare const passwordValidator: (options?: PasswordValidatorOptions) => ValidatorFn;
export declare function passwordValidationMessage(error: any): string;
