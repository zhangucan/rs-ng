import { AfterViewInit, ComponentFactoryResolver, ViewContainerRef } from '@angular/core';
import { NgControl } from '@angular/forms';
export declare class ValidationMessageDirective implements AfterViewInit {
    private componentFactoryResolver;
    private viewContainerRef;
    private ngControl;
    messages: {
        [key: string]: string;
    };
    showError: boolean;
    enableValidation: boolean;
    currentState: string;
    private componentFactory;
    private componentRef;
    constructor(componentFactoryResolver: ComponentFactoryResolver, viewContainerRef: ViewContainerRef, ngControl: NgControl);
    readonly control: import("@angular/forms/src/model").AbstractControl;
    readonly isValid: boolean;
    readonly isInvalid: boolean;
    ngAfterViewInit(): void;
}
