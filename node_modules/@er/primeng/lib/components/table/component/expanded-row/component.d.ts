import { OnChanges, SimpleChanges } from '@angular/core';
import { BaseComponent } from '@er/core';
import { Observable } from 'rxjs/internal/Observable';
import { PngTableColumnProps } from '../../types';
import { PngTableComponent } from '../table/component';
export declare class PngTableExpandedRowComponent extends BaseComponent implements OnChanges {
    pngTable: PngTableComponent;
    rowId: any;
    columns: PngTableColumnProps[];
    styleClass: any;
    style: {} | ((row: any) => {});
    expandContent$: Observable<any>;
    customerContent: boolean;
    constructor(pngTable: PngTableComponent);
    readonly dataColumns: PngTableColumnProps[];
    ngOnChanges(changes: SimpleChanges): void;
    content(col: any, rowData: any): any;
}
