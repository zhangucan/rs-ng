import { EventEmitter, OnInit } from '@angular/core';
import { BaseComponent } from '@er/core';
import { DynaContent, PopupDialogProp } from '@er/types';
import { Observable } from 'rxjs';
export declare class PngPopupDialogComponent extends BaseComponent implements OnInit {
    header: string;
    content: DynaContent;
    canClose: (context: any) => boolean;
    onClose: EventEmitter<any>;
    onAction: EventEmitter<any>;
    dlgOptions$: Observable<PopupDialogProp>;
    templateNames: {
        header: string;
        content: string;
        footer: string;
    };
    options: any;
    ngOnInit(): void;
    resolveDlgOptions(options: any): PopupDialogProp;
    showEvent(dlg: any, options: any): void;
    closeDlg(dlg: any, options: any): void;
}
