import { ChangeDetectorRef, EventEmitter, OnInit } from '@angular/core';
import { BaseComponent } from '@er/core';
import { ConfirmDialogProp } from '@er/types';
import { Confirmation, ConfirmationService } from 'primeng/api';
export declare class PngConfirmDialogComponent extends BaseComponent implements OnInit {
    private confirmationService;
    private cd;
    header: string;
    content: string;
    options: ConfirmDialogProp;
    onAccept: EventEmitter<any>;
    onReject: EventEmitter<any>;
    dlgOptions: ConfirmDialogProp;
    constructor(confirmationService: ConfirmationService, cd: ChangeDetectorRef);
    ngOnInit(): void;
    open(options?: any): void;
    getConfirmation(modal: ConfirmDialogProp): Confirmation;
    accept(): void;
    reject(): void;
}
