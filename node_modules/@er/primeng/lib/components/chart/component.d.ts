import { EventEmitter, OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { BaseComponent } from '@er/core';
import { ApiDataProps, ChartDataProps, ChartDimension, ChartType, DataItemProps, DataSource } from '@er/types';
import { Query } from 'elastic-builder';
import { BehaviorSubject } from 'rxjs';
export declare class PngChartComponent extends BaseComponent implements OnInit, OnChanges {
    $id: string;
    type: ChartType;
    rawData: {}[];
    options: {};
    caption: string;
    captionIcon: string;
    query?: Query | Query[];
    plugins: any[];
    width: string;
    height: string;
    responsive: boolean;
    switchable: boolean;
    dataSource: DataSource | ApiDataProps | ApiDataProps[];
    dataItemProps: DataItemProps;
    dimensions: ChartDimension | ChartDimension[] | ((rawData: any) => ChartDataProps);
    resolveDataSource: {
        method: ((ds: DataSource) => DataSource);
    };
    resolveDimension: {
        method: ((dimension: ChartDimension) => ChartDimension);
    };
    templateNames: {
        header: string;
    };
    onDataSelect: EventEmitter<any>;
    data$: BehaviorSubject<{}>;
    chartTypes: {
        label: string;
        value: ChartType;
        icon: string;
    }[];
    data: {};
    ngOnChanges(changes: SimpleChanges): void;
    changeType(): void;
    reBuildChart(): void;
    buildChart(): void;
    resolvedRawData(rawData: any): void;
}
