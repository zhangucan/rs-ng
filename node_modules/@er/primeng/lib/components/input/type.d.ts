import { ControlAttrs } from '@er/types';
export interface PngInputAttrs extends ControlAttrs {
    $ext: {
        preIcon?: string;
        preText?: string;
        preButtons?: any[];
        floatLabel?: boolean;
        label?: string;
        postIcon?: string;
        postText?: string;
        postButtons?: any[];
        inputRule?: 'pint' | 'int' | 'pnum' | 'num' | 'alpha' | 'alphanum' | RegExp;
    };
}
