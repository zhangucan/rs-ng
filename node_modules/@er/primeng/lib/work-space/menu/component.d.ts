import { OnInit } from '@angular/core';
import { MenuItem } from '@er/types';
import { Observable } from 'rxjs';
import { PngAdminWorkSpaceComponent } from '../component';
export declare class WorkSpaceMainMenuComponent implements OnInit {
    ws: PngAdminWorkSpaceComponent;
    reset: boolean;
    httpMenuTag: any;
    menus$: Observable<MenuItem[]>;
    theme: string;
    layout: string;
    version: string;
    constructor(ws: PngAdminWorkSpaceComponent);
    ngOnInit(): void;
    changeTheme(theme: string): void;
    changeLayout(layout: string, special?: boolean): void;
    changeVersion(version: string): void;
}
