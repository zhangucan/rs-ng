import { DeviceInfo } from '@er/types';
export interface BrowserSupport {
    version?: boolean;
    features?: boolean;
}
export declare class BrowserOsUtils {
    static ua: string;
    static userAgent: string;
    static os: string;
    static browser: string;
    static device: string;
    static os_version: string;
    static browser_version: string;
    static device_info: DeviceInfo;
    static isMobile(): boolean;
    static isTablet(): boolean;
    static isDesktop(): boolean;
    static getDeviceInfo(): DeviceInfo;
    static checkBrowser(): BrowserSupport;
    private static isBrowserVersionValid;
    private static isBrowserFeaturesValid;
}
