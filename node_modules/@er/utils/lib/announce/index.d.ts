import { NotifyProp } from '@er/types';
export declare class AnnounceUtils {
    static info(options: string | NotifyProp): void;
    static error(options: string | NotifyProp): void;
    static success(options: string | NotifyProp): void;
    static warn(options: string | NotifyProp): void;
}
