export declare class ContextUtils {
    static context(date?: any): {};
    static get(key: any): any;
    static resolveVariables(data: any): any;
    private static resolveValue;
    private static resolveJson;
    private static resolveArray;
}
