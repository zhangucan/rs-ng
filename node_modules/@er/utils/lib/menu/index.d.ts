import { MenuItem } from '@er/types';
export declare class MenuUtils {
    static getUserMenuItems(menuItems: MenuItem[], roles?: string | string[], permissions?: string | string[]): MenuItem[];
    static transMenuItems(menuItems: {}[]): any;
}
