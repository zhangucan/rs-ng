import { InjectionToken, Injector, ModuleWithProviders } from '@angular/core';
import { ConfigModel } from '@er/types';
export declare const APP_CONFIG: InjectionToken<string>;
export declare function initialize(appConfig: ConfigModel): () => void;
export declare class ErCoreModule {
    private injector;
    constructor(injector: Injector);
    static forRoot(appConfig: ConfigModel): ModuleWithProviders;
}
