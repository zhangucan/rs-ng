import { DefaultUrlSerializer, UrlTree } from '@angular/router';
export declare class UrlUtils {
    static serializer: DefaultUrlSerializer;
    static parse(url: string): UrlTree;
    static serialize(tree: UrlTree): string;
}
