import { BehaviorSubject } from 'rxjs';
export declare class DateSyncUtils {
    static timeServer$: BehaviorSubject<Date>;
    private static interval;
    static start(): void;
    static updateLocalDate(date: any): void;
    private static stopSyncSchedule;
    private static startRefreshSchedule;
    private static startSyncSchedule;
}
