import { ActivatedRouteSnapshot, RouterStateSnapshot } from '@angular/router';
import { User } from '@er/types';
import { Observable } from 'rxjs';
export declare function anyRoleUser(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): string[] | "NO_ROLE_USER";
export declare class AuthUtils {
    private static CURRENT_USER;
    static login(payLoad: {}, isRememberMe?: boolean): Observable<any>;
    static register(payLoad: {}): Observable<any>;
    static logout(): void;
    static changePwd(payload: any): Observable<any>;
    static getCurrentUser(): User;
    static updateUser(patch: User, remeber?: boolean): void;
    static checkLogin(returnUrl: string): boolean;
    private static onLoginSuccess;
}
