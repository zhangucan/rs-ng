import { InjectionToken, OnDestroy } from '@angular/core';
import { Task } from '@er/types';
import { Subscription } from 'rxjs';
export declare const BACKGROUND_TASK_TOKEN: InjectionToken<Task>;
/**
 * used to run background task globally
 */
export declare class TaskService implements OnDestroy {
    subs: Subscription[];
    constructor(tasks?: Task[]);
    updateTitle(): void;
    cleanStates(): void;
    startTask(task: Task): void;
    ngOnDestroy(): void;
}
