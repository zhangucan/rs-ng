import { ElementRef, EventEmitter, Renderer2, SimpleChanges, Type, ViewContainerRef } from '@angular/core';
export declare class DynaCompComponent {
    private viewContainerRef;
    private el;
    private renderer;
    type: Type<any> | string;
    props: {};
    onCreated: EventEmitter<{}>;
    private component;
    constructor(viewContainerRef: ViewContainerRef, el: ElementRef, renderer: Renderer2);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private createComponent;
}
