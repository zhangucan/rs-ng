import { ChangeDetectorRef } from '@angular/core';
import { BaseComponent } from '../../bases/component';
export declare class AnimateNumberComponent extends BaseComponent {
    private cd;
    currentNumber: number;
    fontSize: string;
    private counter$;
    constructor(cd: ChangeDetectorRef);
    private _end;
    end: number;
    private positiveOrNegative;
    private takeUntil;
}
