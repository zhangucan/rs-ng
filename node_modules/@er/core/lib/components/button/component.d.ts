import { ChangeDetectorRef, EventEmitter, OnInit } from '@angular/core';
import { Subject } from 'rxjs';
import { BaseComponent } from '../../bases/component';
export declare class StateButtonComponent extends BaseComponent implements OnInit {
    protected cd: ChangeDetectorRef;
    type: 'button' | 'link';
    hint: string;
    label: string;
    icon: string;
    style: {};
    hidden: boolean;
    styleClass: string;
    disabled: boolean;
    iconRight: boolean;
    stateKey: string;
    stateLabel: string;
    stateHint: string;
    context: {};
    onClick: EventEmitter<any>;
    onFocus: EventEmitter<any>;
    onBlur: EventEmitter<any>;
    processIcon: string;
    processing$: Subject<boolean>;
    constructor(cd: ChangeDetectorRef);
    processing: boolean;
    readonly btnLabel: string;
    readonly btnTitle: string;
    ngOnInit(): void;
    click(event: any): void;
}
