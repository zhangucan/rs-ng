import { EventEmitter } from '@angular/core';
import { BaseComponent } from '../../../bases/component';
export declare class LeftRightContainerComponent extends BaseComponent {
    leftStyleClass: string;
    rightStyleClass: string;
    hideLeft: boolean;
    switchable: boolean;
    onLeftHide: EventEmitter<boolean>;
    templateNames: {
        left: string;
        right: string;
    };
    reLayout(event: any): void;
}
