import { OnInit } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';
import { Breadcrumb } from '@er/types';
import { Observable } from 'rxjs';
import { BaseComponent } from '../../bases/component';
export declare class BreadcrumbsComponent extends BaseComponent implements OnInit {
    private router;
    private route;
    outlet: string;
    breadcrumbs$: Observable<Breadcrumb[]>;
    constructor(router: Router, route: ActivatedRoute);
    ngOnInit(): void;
    getRouterLink(breadcrumb: Breadcrumb): (string | import("@angular/router/src/shared").Params)[];
    private resolveRoute;
}
