import { EventEmitter } from '@angular/core';
import { QueryRule } from '@er/types';
import { BaseComponent } from '../../../bases/component';
import { QueryBuilderComponent } from '../component';
export declare class QueryBuilderRuleComponent extends BaseComponent {
    qb: QueryBuilderComponent;
    ruleIndex: number;
    ruleChange: EventEmitter<{}>;
    constructor(qb: QueryBuilderComponent);
    private _rule;
    rule: QueryRule;
}
