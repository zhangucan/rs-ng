import { ChangeDetectorRef, OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { ContentType, Context, DynaComponent, DynaContent } from '@er/types';
import { Observable } from 'rxjs';
import { BehaviorSubject } from 'rxjs/internal/BehaviorSubject';
import { BaseComponent } from '../../bases/component';
export declare class DynaContentComponent extends BaseComponent implements OnInit, OnChanges {
    cd: ChangeDetectorRef;
    CONTENT_TYPE: typeof ContentType;
    content: DynaContent;
    context: Context;
    styleClass: any;
    style: any;
    parsedContent: any;
    parsedContent$: BehaviorSubject<string>;
    contentType: ContentType;
    parsedContext: Context;
    parsedContextFn: (context: any) => string | Observable<string>;
    dynaComponent: DynaComponent;
    constructor(cd: ChangeDetectorRef);
    ngOnChanges(changes: SimpleChanges): void;
    refresh(): void;
    private parse;
}
