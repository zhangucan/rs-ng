import { OnInit, ViewContainerRef } from '@angular/core';
import { ConfigModel } from '@er/types';
import { BrowserSupport } from '@er/utils';
import { Observable } from 'rxjs';
export declare abstract class AppConfigComponent implements OnInit {
    rootViewContainerRef: ViewContainerRef;
    httpState$: Observable<boolean>;
    routeState$: Observable<boolean>;
    inValidBrowser: boolean;
    constructor(rootViewContainerRef: ViewContainerRef);
    readonly loadingClass: string;
    ngOnInit(): void;
    resolveBrowserSupport(browserSupport: BrowserSupport): void;
    abstract appConfig(): ConfigModel;
}
