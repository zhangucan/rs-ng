import { ChangeDetectorRef, ElementRef, OnChanges, SimpleChanges } from '@angular/core';
import { BaseDirective } from '../../bases/directive';
export { ObjectDiff } from './diff';
export declare class JsonViewerDirective extends BaseDirective implements OnChanges {
    private el;
    private cd;
    model: {};
    showCurrent: boolean;
    openLevel: number;
    showDiff: boolean;
    private diff;
    constructor(el: ElementRef, cd: ChangeDetectorRef);
    ngOnChanges(changes: SimpleChanges): void;
    showJson(): void;
}
