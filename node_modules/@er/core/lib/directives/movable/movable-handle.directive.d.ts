import { ElementRef } from '@angular/core';
export declare class MovableHandleDirective {
    element: ElementRef;
    movableEnabled: boolean;
    isMoving: boolean;
    movableHandle: string;
    protected isHandle: boolean;
    constructor(element: ElementRef);
}
