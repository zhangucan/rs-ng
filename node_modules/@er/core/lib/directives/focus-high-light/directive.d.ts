import { ElementRef } from '@angular/core';
import { BaseDirective } from '../../bases/directive';
export declare class FocusHighlightDirective extends BaseDirective {
    private el;
    bgColor: string;
    defaultBgColor: string;
    constructor(el: ElementRef);
    onMouseEnter(): void;
    onMouseLeave(): void;
    private highlight;
}
