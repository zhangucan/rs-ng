import { AfterViewChecked, AfterViewInit, ElementRef, OnChanges, SimpleChanges } from '@angular/core';
import { BaseDirective } from '../../bases/directive';
export declare class HighlightDirective extends BaseDirective implements OnChanges, AfterViewInit, AfterViewChecked {
    private el;
    searchTerm: any;
    caseSensitive: boolean;
    private viewRendered;
    private WRAPPER_TOKEN;
    constructor(el: ElementRef);
    private readonly caseSensitivity;
    ngOnChanges(changes: SimpleChanges): void;
    ngAfterViewInit(): void;
    ngAfterViewChecked(): void;
    highlightSearchTerm(): void;
    private markMatchedTextTokens;
    private markMatchedTextAndRemoveUniqueTokens;
    private traverseNodesInElement;
    private wrapUniqueTokensAroundMatchedText;
    private traverseHtmlElementsTree;
    private removePreviouslyMarkedTextInNode;
    private getSearchTerm;
    private getUniqueTokenWrappedSearchTerm;
    private escapeRegExp;
}
