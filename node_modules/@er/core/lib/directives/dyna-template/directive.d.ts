import { ElementRef, OnInit, Renderer2, ViewContainerRef } from '@angular/core';
import { Context } from '@er/types';
import { BaseDirective } from '../../bases/directive';
export declare class DynaTemplateDirective extends BaseDirective implements OnInit {
    private viewContainerRef;
    private el;
    private renderer;
    template: string;
    context: Context;
    private componentRef;
    constructor(viewContainerRef: ViewContainerRef, el: ElementRef, renderer: Renderer2);
    ngOnInit(): void;
    ngOnDestroy(): void;
}
