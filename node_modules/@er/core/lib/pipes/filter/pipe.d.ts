import { PipeTransform } from '@angular/core';
export declare class FilterPipe implements PipeTransform {
    transform(arr: any[], criteria: {
        key: string;
        value: any;
    } | any, matcher?: {
        startWith: boolean;
        contains: boolean;
        regex: boolean;
        pinyin: boolean;
    }): any[];
}
