import { ChangeDetectorRef, NgZone, OnDestroy, PipeTransform } from '@angular/core';
export declare class UpToNowPipe implements PipeTransform, OnDestroy {
    private cd;
    private ngZone;
    private timer;
    constructor(cd: ChangeDetectorRef, ngZone: NgZone);
    transform(value: any, due?: Date): string;
    ngOnDestroy(): void;
    private removeTimer;
}
