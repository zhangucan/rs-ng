import { PipeTransform } from '@angular/core';
export declare class IdCardPipe implements PipeTransform {
    transform(idCardNo: any, attr?: 'gender' | 'birthday' | 'age'): string | number | {
        gender?: string;
        birthday?: string;
        age?: number;
    };
}
