import { ElementRef, PipeTransform } from '@angular/core';
export declare class UnitNumberPipe implements PipeTransform {
    private ele;
    constructor(ele: ElementRef);
    transform(value: number, decimal?: number): string;
}
