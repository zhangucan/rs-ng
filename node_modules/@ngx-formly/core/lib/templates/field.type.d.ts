import { FormGroup } from '@angular/forms';
import { FormlyFieldConfig } from '../components/formly.field.config';
export declare abstract class FieldType<F extends FormlyFieldConfig = FormlyFieldConfig> {
    field: F;
    defaultOptions?: F;
    model: any;
    form: FormGroup;
    options: F['options'];
    readonly key: string;
    readonly formControl: F['formControl'];
    readonly to: F['templateOptions'];
    readonly showError: boolean;
    readonly id: string;
    readonly formState: any;
}
/**
 * @deprecated use `FieldType` instead
 */
export declare abstract class Field extends FieldType {
    constructor();
}
