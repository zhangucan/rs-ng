import {settings} from './settings';
import {rate} from './rate';
import {employees} from './employees';
import {finalWageData} from './finalWageData';

export const pageProps = {
  settings,
  rate,
  employees,
  finalWageData
};
